# This file is consumed by the Probot Settings app.
# GitHub does not apply branch protection from repository files natively.

branches:
  - name: main
    protection:
      required_status_checks:
        strict: true
        contexts:
          - backend-quality
      enforce_admins: false
      required_pull_request_reviews: null
      restrictions: null
